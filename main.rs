fn main() {
    let x: i32 = 3;
    // let y: i32;

    assert_eq!(x,5);
    println!("Success!");

    let mut z = 1;
    x = x + 2; // 3 

    // Most of the time type declarations aren't necessary
}